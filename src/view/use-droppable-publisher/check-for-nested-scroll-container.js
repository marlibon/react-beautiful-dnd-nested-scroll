// @flow
// Nested scroll containers are now supported.
// This file is kept for backward compatibility but no longer emits a warning.

export default (_scrollable: ?Element) => {
  // No-op: nested scroll containers are now fully supported
};
